// SCREEN 1 — Student Dashboard: stat cards, search bar, catalog cards, current borrowings + history tables.
// ── Write this screen/component's code below. ──

export default function StudentDashboardPage() {
  return (
    <div>
      {/* TODO: StudentDashboardPage */}
    </div>
  );
}
