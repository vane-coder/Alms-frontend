// Filters — search + genre/author/availability bar (Screen 1).
// ── Write this screen/component's code below. ──

export default function SearchFilterBar() {
  return (
    <div>
      {/* TODO: SearchFilterBar */}
    </div>
  );
}
